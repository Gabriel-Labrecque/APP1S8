services:
  api:
    build:
      context: ./CoupDeSonde.Api
    ports:
      - "8088:8080"
    environment:
      - ASPNETCORE_URLS=http://+:8080
      - ASPNETCORE_ENVIRONMENT=Development
      - ApiKeys__0=${API_KEY:-REMPLACER-PAR-UNE-CLE-GENEREE-EN-DEV}
